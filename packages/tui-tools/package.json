{
  "name": "@supierior/tui-tools",
  "version": "0.1.0-beta.0",
  "description": "Reusable Pi TUI question primitives for guided workflows.",
  "keywords": [
    "pi",
    "tui",
    "questions"
  ],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    },
    "./questions": {
      "types": "./dist/questions.d.ts",
      "import": "./dist/questions.mjs",
      "require": "./dist/questions.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "pnpm run lint:deps",
    "lint:deps": "depcruise extension-src --config scripts/.dependency-cruiser.cjs",
    "clean": "rimraf dist coverage .turbo",
    "prepack": "pnpm run build"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "dependency-cruiser": "^16.10.4",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.1",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  }
}
