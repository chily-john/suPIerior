{
  "name": "@supierior/experimental-workflows",
  "version": "0.1.0-beta.0",
  "description": "Experimental Workflower playground package for impractical workflows that demonstrate engine patterns.",
  "keywords": [
    "pi-package",
    "pi",
    "workflow",
    "workflower",
    "experimental",
    "playground"
  ],
  "license": "Apache-2.0",
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist",
    "extension-src/experimental-workflows/internals/skills",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "pnpm run lint:deps",
    "lint:deps": "depcruise extension-src --config scripts/.dependency-cruiser.cjs",
    "clean": "rimraf dist coverage .turbo",
    "prepack": "pnpm run build"
  },
  "dependencies": {
    "@supierior/workflower": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "dependency-cruiser": "^16.10.4",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.1",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./dist/index.mjs"
    ],
    "workflowerSkills": [
      "./extension-src/experimental-workflows/internals/skills"
    ]
  }
}
